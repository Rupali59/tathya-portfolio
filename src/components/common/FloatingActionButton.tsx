"use client";

// FloatingActionButton is now handled by Navigation component's bottom command bar
// This component is kept for backward compatibility but renders nothing
export default function FloatingActionButton(): JSX.Element {
  return <></>;
}
